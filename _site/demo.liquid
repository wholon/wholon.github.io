{% assign cat = page.url | split: "\\" | last %}
{{ page.url | split: "\\" | last }}
{{ site.categories.cat }}
{{ cat }}
{% if condition %}

{% endif %}
{% assign nav = value %}

{% for headenv in site.data.navigation.header %}
  {% if headenv.url == page.url %}
    {% assign page.title = headenv.titles.zh %}
  {% endif %}
{% endfor %}

{% for categorie in site.categories %}
  {% if categorie[0] != site.categories.first[0] %}
    ,
  {% endif %}
  "{{ categorie[0] }}":[
  {% for post in categorie[1] %}
    {% if post != categorie[1].first %}
      ,
    {% endif %}
    {"url":"{{ post.url }}",
    "title":"{{ post.title }}",
    "date":"{{ post.date | date: "%d/%m/%Y" }}"
    }
  {% endfor %}]
{% endfor %}
